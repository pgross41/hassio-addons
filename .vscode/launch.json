{
  "configurations": [
    {
      "name": "Docker: Python - email2dropbox",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: email2dropbox",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/app",
            "remoteRoot": "/app"
          }
        ],
        "projectType": "general"
      }
    },
    {
      "name": "Docker: Python - email2email",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: email2email",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/app",
            "remoteRoot": "/app"
          }
        ],
        "projectType": "general"
      }
    }
  ]
}
